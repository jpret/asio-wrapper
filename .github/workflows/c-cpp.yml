name: C/C++ CI

on:
  push:
    branches: [ development/main ]
  pull_request:
    branches: [ development/main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build setup
      shell: bash
      run: |
        pwd
        ls -l
        mkdir -p asio-wrapper/build
        cd asio-wrapper/build
        pwd
        ls -l
        cmake --version
        make --version
        g++ --version
